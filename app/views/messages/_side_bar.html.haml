.side-bar
  .user-name
    .user-name__top-items
      = current_user.name
      %p.user-name__top-items__name 
      %ul.user-name__top-items__menu
        %li
          = link_to new_group_path(current_user), class: "a" do
            = icon('fas', 'edit')
          = link_to edit_user_path(current_user), class: "a" do
            = icon('fas', 'cog')
  .group-list
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。
        

